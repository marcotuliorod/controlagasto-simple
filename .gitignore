# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variáveis de ambiente — nunca versionar (ver .env.example)
.env
.env.*
!.env.example

# Scratch do CLI do Supabase (gerado por `supabase start`)
supabase/.temp/
supabase/.branches/

# Saída do Playwright (inclui o storageState autenticado — nunca versionar)
artifacts/
test-results/
playwright-report/

# Duplicatas criadas pelo iCloud Drive ("arquivo 2.ts"). Além de poluírem o
# repo, quebram o `supabase start`: duas migrations com o mesmo timestamp
# violam a PK de schema_migrations.
* 2.*
* [0-9].*
* [0-9]
*.icloud

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
